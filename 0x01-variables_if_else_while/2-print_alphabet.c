#include <stdio.h>
/**
 * the mai function to print the alpha.in lowercase
 * th return must be in zero to success
 */
int main(void)
{
	char ch;

	for (ch= 'a',ch<= 'z',ch++)
	{
		putchar(ch);
	}
	putchar("\n");
	return (0);
}
	

