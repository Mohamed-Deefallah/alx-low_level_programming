#include "main.h"
/**
 *_islower - fuction to print liower case
 * @c: character to make checking
 * Return: 1 if lowe cas and 0 if uppercase
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
