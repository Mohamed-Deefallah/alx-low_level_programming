#include "main.h"
/**
 * mul - the multip function
 * @a: the first number
 * @b: the second number
 * Return: the result of operation
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
